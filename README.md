# static-currency-converter

This project uses pure HTML, CSS and JAVASCRIPT. It is a static currency converter, meaning the currency values ​​are fixed inside the code. 

Este projeto usa HTML, CSS e JAVASCRIPT puros. É um conversor de moedas estático, ou seja, os valores das moedas estão fixados dentro do código.
